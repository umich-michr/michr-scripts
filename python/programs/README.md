# Python programs

Independently runnable Python applications belong here.

Programs may compose reusable libraries from `python/packages/` and own their
CLI, configuration, logging, orchestration, and external I/O.
